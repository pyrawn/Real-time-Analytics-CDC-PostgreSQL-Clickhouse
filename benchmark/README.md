# benchmark/ — owner: Leonora

See [docs/handoffs.md](../docs/handoffs.md) Section 4 for the full task.

Put here: `queries/*.pg.sql` + `queries/*.ch.sql` pairs, `run_benchmark.py` (or
script), and `results/` with captured `EXPLAIN` output and `summary.md`.
